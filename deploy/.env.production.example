# Deprecated alias — use deploy/.env.vps.example (canonical VM .env).
# SSO notes: deploy/AUTH-SSO-SETUP.md
#
# Auth switch: password | sso | both
#
# Azure Entra (direct OAuth — secrets in capexbe/.env / VPS .env):
#   AZURE_TENANT_ID / AZURE_CLIENT_ID / AZURE_CLIENT_SECRET
#   Redirect URI (Web) = Capex callback, e.g.:
#     http://localhost:3000/api/auth/azure/callback
#     https://capex.cgp-ai.com/auth/callback
